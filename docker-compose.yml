version: '3'

services:
  redis:
    image: redis
    container_name: redis
    restart: unless-stopped
    networks:
      - internal
    ports:
      - 6379:6379

networks:
  internal:
    external: true
